<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@android:color/white"/>
    <size android:width="24dp" android:height="24dp"/>
    <corners android:radius="12dp"/> <!-- Round corners to create circular shape -->
    <stroke android:color="@android:color/white" android:width="2dp"/> <!-- Border for the outline -->
</shape>
